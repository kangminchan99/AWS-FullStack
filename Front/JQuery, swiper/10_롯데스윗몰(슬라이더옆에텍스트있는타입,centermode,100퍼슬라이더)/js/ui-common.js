$(function () {
  let mainNew = new Swiper('.main_new .swiper', {
    // css로 크기지정시 auto
    slidesPerView: 'auto',
    spaceBetween: 30,
    // And if we need scrollbar
    scrollbar: {
      el: '.swiper-scrollbar',
    },
  });
});
